# Checkout Authentication Architecture

`checkout-api` validates JWTs using a production issuer and shared identity configuration. Deployment configuration must remain aligned with the production identity provider hostname.
